pipeline {
  agent { label 'nix' }

  triggers {
    pollSCM('H/5 * * * *')
  }

  stages {
    stage('prepare') {
      steps {
        sh "nix --version"
      }
    }

    stage('build') {
      steps {
        script {
          def flakeJson = sh(returnStdout: true, script: 'nix flake show --json')
          def flake = readJSON(text: flakeJson)
          flake.packages."x86_64-linux".each { name, _ ->
            sh "nix build .#${name}"
          }
        }
      }
    }

    stage('test') {
      steps {
        dir("test") {
          // prevents `fetching git input 'git+file://file:..'` from happening
          sh "rm flake.lock"
          sh "nix flake check"
        }
      }
    }
  }
}
