pipeline {
   agent { label 'nix' }

    triggers {
        pollSCM('H/1 * * * *')
    }

   stages {
       stage('Check') {
           steps {
               sh 'nix flake check --no-build'
           }
       }
   }
 }

